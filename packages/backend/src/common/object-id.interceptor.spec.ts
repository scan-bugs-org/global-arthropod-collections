import { ObjectIdInterceptor } from './object-id.interceptor';

describe('ObjectIdInterceptor', () => {
  it('should be defined', () => {
    expect(new ObjectIdInterceptor()).toBeDefined();
  });
});
