{% extends "webpage.nunjucks" %}

{% block body %}
  <div class="text-center">
    <p class="text-danger">{{ errorMsg | default("Unknown error") }}</p>
    <a href="{{ backUrl | default('..') }}">Go Back</a>
  </div>
{% endblock %}