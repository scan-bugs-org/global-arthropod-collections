{% extends "webpage.nunjucks" %}

{% block body %}
  <div class="text-center p-5">
    <p class="text-danger">{{ error | default("Unknown error") }}</p>
    <a href="{{ backUrl | default('..') }}">Go Back</a>
  </div>
{% endblock %}